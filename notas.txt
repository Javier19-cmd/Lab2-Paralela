El ejercicio dice que se use time, pero modificar el codigo para que use otra herramienta para timing.
Si la compu hace muy facil 10k de pruebas, subirle de tamano.
Se puede hardcodear una matriz en un archivo y leerlo en vez de generar una matriz como lo hace el codigo.
